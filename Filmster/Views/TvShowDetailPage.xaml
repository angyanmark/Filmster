<Page x:Class="Filmster.Views.TvShowDetailPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      Style="{StaticResource PageStyle}"
      mc:Ignorable="d">

    <StackPanel>
        <TextBlock Text="{x:Bind ViewModel.TvShow.Name, Mode=OneWay}" />
        <FlipView ItemTemplate="{StaticResource FlipPosterTemplate}"
                  ItemsSource="{x:Bind ViewModel.TvShow.Images.Posters, Mode=OneWay}"
                  Style="{StaticResource FlipView}" />
    </StackPanel>
</Page>
